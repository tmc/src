# ctx-llm-txt

A tool that serves up source code or web content as plain text files optimized for language models.

## Features

- Serve source code files as plain text
- Fetch and serve web content as plain text
- Caching to avoid repeated fetches/reads
- Simple HTTP API

